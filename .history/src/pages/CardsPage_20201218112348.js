import React from 'react';
import Cards from '../components/Components/Cards';

const SectorsPage = () => {
  return (
    <React.Fragment>
      <Sectors />
    </React.Fragment>
  );
};

export default SectorsPage;
