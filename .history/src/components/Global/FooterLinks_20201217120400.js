import React from 'react';

const FooterLinks = (props) => {
  console.log(props);

  const renderLinks()this.props.map((link) => {
      return ( 
          <li>test</li>
      );
    });
    </ul>
  }

  return <div>{this.renderLinks()}</div>;
};

export default FooterLinks;
