import styled from 'styled-components';

const Row = styled.div`
  width: calc(100% - 30px);
  max-width: 1200px;
  margin: 0 auto;
  padding: 0;
  position: relative;
  padding: 0 15px;
`;

export const Styled = {
  Row
};
